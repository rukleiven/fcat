language: python

before_script:
  - pip install .
  - python --version

stages:
    - Static Analysis
    - Code Test

flake8:
    stage: Static Analysis
    script:
        - pip install flake8
        - flake8 fcat/

pytests:
    stage: Code Test
    script:
        - pip install pytest
        - pytest tests/